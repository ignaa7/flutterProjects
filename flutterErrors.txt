-error shown:
The current Dart SDK version is 3.1.0.

Because flutter_chat requires SDK version >=3.1.2 <4.0.0, version solving failed.


You can try the following suggestion to make the pubspec resolve:
* Try using the Flutter SDK version: 3.13.7.

1ªsol:
flutter upgrade
flutter pub get

2ªsol:
flutter update --force:


-flutter pub get:
install needed dependencies to run a project (like npm install)